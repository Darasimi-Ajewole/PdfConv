name: Deploy to Cloud run and Firebase
on:
  push:
    branches:
      - master
jobs:
  setup-build-publish-deploy-firebase:
    name: Setup, Build, Publish, and Deploy assets to Firebase
    runs-on: ubuntu-latest
    steps:
    - name: Build Front end assets
      env:
        CLOUD_RUN_ACCOUNT: ${{ secrets.GCP_EMAIL }}
        GCP_PROJECT: ${{ secrets.GCP_PROJECT }}
      run: |
        cd assets
        yarn install &&
        yarn build && 
        cp -r build ../firebase

    - name: Deploy to firebase
      uses: FirebaseExtended/action-hosting-deploy@v0
      with:
        repoToken: '${{ secrets.GITHUB_TOKEN }}'
        firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_KEY }}'
        projectId: pdf-conv-3d1f5
        channelId: live
        entryPoint:  ./firebase

  setup-build-publish-deploy-cloudrun:
    name: Setup, Build, Publish, and Deploy PDF service to cloud run
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    # Setup gcloud CLI
    - uses: google-github-actions/setup-gcloud@master
      with:
        project_id: ${{ secrets.GCP_PROJECT }}
        service_account_key: ${{ secrets.SERVICE_ACCOUNT_CREDENTIALS }}
        export_default_credentials: true
    
    # Configure Docker with Credentials
    - name: Configure Docker
      run: |
        gcloud auth configure-docker

    - name: Deploy to cloud run
      uses: google-github-actions/deploy-cloudrun@main
      with:
        service: default
        image: gcr.io/${{ secrets.GCP_PROJECT }}/pdf-conv:latest
        credentials: ${{ secrets.SERVICE_ACCOUNT_CREDENTIALS }}
        region: us-central1

    - name: Use Output
      run: curl "${{ steps.deploy.outputs.url }}"
